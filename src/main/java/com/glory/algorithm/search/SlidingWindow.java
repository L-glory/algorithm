package com.glory.algorithm.search;

/**
 * 滑动窗口
 *
 * @author Glory
 * @create 2020-08-13 23:20
 **/
public class SlidingWindow {

   /* public void base(int[] arr) {
        int len = arr.length;
        int left = 0;
        int right = 0;
        while (right < len) {
            window.add(arr[right]);
            right++;
        }
        while (valid) {
            window.remove(arr[left]);
            left++;
        }
    }*/
}
